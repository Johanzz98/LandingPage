import React from 'react';
import { Box, Typography } from '@mui/material';

const Navbar = () => {
  return (
    <Box>
      <Typography variant="h6">Navba aca</Typography>
    </Box>
  );
};

export default Navbar;
